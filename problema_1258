#include <stdio.h>
#include <string.h>

typedef struct{
    char nome[250];
    char cor[10];
    char tamanho;
}blusas;

void ordenarBlusas(blusas lista[], int total){

}

int main() {
    int i, n, j;
    scanf("%d", &n);
    while(n != 0){
        blusas turma[n];
        for(i=0;i<n;i++){
            scanf(" %[^\n]", turma[i].nome); 
            scanf("%s %c", turma[i].cor, &turma[i].tamanho);
        }
        ordenarBlusas(turma, n);

        
        printf("\n");
        scanf("%d", &n);
    }
    return 0;
}
